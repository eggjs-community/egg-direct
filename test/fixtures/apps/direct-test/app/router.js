/* eslint-disable no-unused-vars */
'use strict';

module.exports = app => {
  /** There is no route configured here.
   *
   *      const { router, controller } = app;
   *      router.get('/', controller.home.index);
   */
};
